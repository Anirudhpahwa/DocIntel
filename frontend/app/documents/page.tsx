import DocumentExplorer from "@/components/document/DocumentExplorer";

/** Documents workspace (Phase 6): browse/upload/delete/inspect, reusing the existing explorer. */
export default function DocumentsPage() {
  return <DocumentExplorer />;
}
